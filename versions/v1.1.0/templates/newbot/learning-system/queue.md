# {DOMAIN} - Queue (Active Sessions)

> Read every session. Lightweight by design: a learning domain's real state lives in `{DOMAIN}_PROGRESS.md` (what's been covered) and `{DOMAIN}_COURSEWORK.md` (per-lesson record). This queue is for the immediate next-action and the small handful of TODOs that have surfaced between sessions. Keep it under ~80 lines.

**Last updated:** {DATE}

## Quick Resume

The thirty-second orientation. Where things stand, what is the single most important next study action.

- **State:** Setting up. {DOMAIN_TITLE} learning scaffold just landed. Curriculum source picked.
- **Next:** Fill in `{DOMAIN}_TOPIC.md` with the real curriculum, then start session 1.
- **Watch:** Nothing yet - this is a fresh domain.

## This Session

Plans for the current study session. Write before doing. Check off as completed. Carry the granular outputs into `{DOMAIN}_COURSEWORK.md` and the session summary into `{DOMAIN}_PROGRESS.md`.

- [ ] {First concrete study task - replace this placeholder, e.g. "read chapter 2 sections 2.1 - 2.3"}

## Active

Multi-session threads. Bigger units of work that span more than a single sitting - a long project, a hard topic taking multiple passes, a section being slowly worked through.

- {Active thread - what it is, where it stands, what unblocks completion}

## Queue

Study tasks queued for future sessions, ordered roughly by priority. The next session pulls from the top.

- [ ] {Queued task - e.g. "module 3 exercises 3.4 - 3.6"}

## Stuck / Blocked

Topics or tasks that hit a wall and need something external before they can move - a prerequisite not yet covered, a tool not yet installed, a question that needs asking elsewhere.

- {Stuck item - what blocks it, what would unblock it}

## Recently Completed

Last few study sessions or finished tasks. Newest first. When this passes ~5 items, rotate the oldest into `{DOMAIN}_PROGRESS.md` (Sessions section) and/or `{DOMAIN}_LOG.md` if you keep one.

- {Completed item} ({DATE})
